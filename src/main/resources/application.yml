server:  
  port: ${PORT:9999}

spring:  
  application:
    name: auth-server
  datasource:
    url: jdbc:postgresql://localhost:33306/security?useSSL=false
    username: crm_1
    password: BVfBymMVpTVZ
    
logging:
  level:
    org.springframework.security: TRACE
      
security:
#  basic:
#    enabled: false
  user:
    name: root
    password: password
  oauth2:
    client:
      client-id: acme
      client-secret: secret
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://51.68.44.179:8700/eureka/
